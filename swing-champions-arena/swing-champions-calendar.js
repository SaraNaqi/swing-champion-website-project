import { LitElement, html } from "lit";

export class SwingChampionsCalendar extends LitElement{
    static get tag() {
        return "swing-champions-caledar";
    }

    render() {
        return html`
        <p>idk yet </p>
        `;
    }
}
globalThis.customElements.define(SwingChampionsCalendar.tag, SwingChampionsCalendar);
